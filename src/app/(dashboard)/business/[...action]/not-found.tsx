import NotFound from '@/components/NotFound';

export default function BusinessNotFound() {
  return (
    <NotFound>
      {`Sorry, but we couldn't find that particular Business`}
    </NotFound>
  );
}
