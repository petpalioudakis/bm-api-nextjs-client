import NotFound from '@/components/NotFound';

export default function StaffNotFound() {
  return (
    <NotFound>
      {`Sorry, but we couldn't find that particular Staff member`}
    </NotFound>
  );
}
